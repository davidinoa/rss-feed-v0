// Categories module — populated in slice #59 (edit Subscription with
// Categories). This stub exists so the Convex `api.categories` namespace
// anchors now; the slice that fills it won't need to touch routing.
export {}
